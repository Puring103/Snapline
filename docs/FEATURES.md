# 功能状态

状态说明：`计划`、`进行中`、`完成`。

| 模块 | 状态 | 验收证据 |
| --- | --- | --- |
| M0 工程与契约 | 完成 | `cargo fmt`、`cargo clippy`、`cargo test` 全部通过 |
| M1 服务端认证与设备 | 完成 | 真实 PostgreSQL HTTP 生命周期测试通过 |
| M2 服务端加密同步 | 完成 | 幂等、隔离、冲突、pull/ack 真实 PostgreSQL 测试通过 |
| M3 服务端附件 | 完成 | 真实 PostgreSQL 分片、状态、哈希、下载、用户隔离、原子配额、过期清理和幂等删除测试通过 |
| M4 myServer 部署 | 完成 | 数据库就绪探针、升级前备份、相对哈希清单、隔离恢复、隔离回滚及公网健康检查通过 |
| M5 桌面本地存储与加密 | 完成 | 原生注册/登录、Windows 凭据、UMK/DEK、记录与分块附件加密测试及真实 myServer 登录通过 |
| M6 Markdown 与快速记录 | 完成 | 完整 Markdown、选区格式化、撤销/重做、粘贴图片、自动保存、全局快捷键、托盘、独立记录窗口及单窗口复用通过测试 |
| M7 桌面媒体 | 完成 | 原生主屏截图、麦克风 WAV 录音、图片/视频流式加密导入、自定义协议 Range 预览与严格 ACL/CSP 通过自动化验收 |
| M8 历史、标签和特殊标记 | 完成 | 历史抽屉、50 条分页、收藏/归档/删除、普通标签、自定义特殊标记及来源/标签/标记组合筛选通过测试 |
| M9 单模型多模态处理 | 完成 | Windows 凭据配置、图片/音频能力探测、持久处理队列、严格 JSON Schema、视频关键帧/音轨、加密元数据及内存 FTS5 通过测试 |
| M10 Agent 搜索与对话 | 完成 | 7 个白名单只读工具、多轮限制、提示注入边界、可点击真实引用及对话错误恢复通过测试 |
| M11 桌面同步闭环 | 完成 | durable outbox、令牌轮换、双向增量、附件续传、附件回收、冲突选择、撤销响应及真实 myServer 双设备测试通过 |

Windows 交付物为 NSIS x64 安装包。安装、卸载和文件完整性冒烟通过；当前没有代码签名证书，因此安装包会显示未知发布者，正式分发前应配置 Authenticode 签名。
